services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    env_file: .env
    ports:
      - ${APP_PORT}:${APP_PORT}
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start:dev
